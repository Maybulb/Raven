<form action="/edit/{{poem._id}}" method="post">
	<input name="title" type="text" value="{{poem.title}}">
	<textarea name="content">{{poem.content}}</textarea>

	<input type="submit" value="Save">
</form>